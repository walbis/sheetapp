# This file makes the 'app' directory a Python package.
# It can be left empty or used to define package-level variables/imports if needed.

default_app_config = 'app.apps.AppConfig' # Optional: Specify default AppConfig
# Example environment variables for the frontend (Create React App specific).
# Variables MUST start with REACT_APP_ to be included in the build.
# Copy this file to .env and modify values for local development outside Docker if needed.

# URL for the backend API server
# This value is typically injected by docker-compose from the root .env file when running in Docker.
REACT_APP_API_URL=http://localhost:8000/api

# Example of another frontend-specific variable
# REACT_APP_GOOGLE_ANALYTICS_ID=UA-XXXXX-Y
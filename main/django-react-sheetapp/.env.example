# PostgreSQL Database Settings
DB_ENGINE=django.db.backends.postgresql
DB_NAME=sheetapp_db
DB_USER=sheetapp_user
DB_PASSWORD=verysecretpassword123 # Use a strong password in production
DB_HOST=db
DB_PORT=5432

# Django Settings
DJANGO_SECRET_KEY= # Generate a strong secret key for production (e.g., using Python's secrets module or online generator)
DEBUG=True # Should be False in production

# CORS Settings - Comma-separated list of allowed origins (adjust for deployment)
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# React Frontend Settings
REACT_APP_API_URL=http://localhost:8000/api # URL for the Django backend API